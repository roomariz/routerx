{
  "_notes": [
    "RouterX validates this file at startup. Missing or invalid required fields will stop the CLI.",
    "Keys prefixed with '_' are informational and can be removed from your own configuration."
  ],
  "_requiredFields": {
    "defaultModel": "Required non-empty string (e.g. openai/gpt-4o-mini).",
    "defaultBaseUrl": "Required valid URL (e.g. https://openrouter.ai/api/v1).",
    "defaultSavePath": "Required writable directory path.",
    "timeout": "Required positive number in milliseconds.",
    "maxRetries": "Required non-negative integer.",
    "resilience": {
      "timeoutMs": "Required positive number in milliseconds.",
      "maxRetries": "Required non-negative integer.",
      "baseDelayMs": "Required positive number in milliseconds.",
      "maxDelayMs": "Required positive number in milliseconds and must be >= baseDelayMs.",
      "jitterMs": "Required non-negative number in milliseconds.",
      "breakerThreshold": "Required positive integer.",
      "breakerCooldownMs": "Required positive number in milliseconds.",
      "breakerHalfOpenSuccesses": "Required positive integer.",
      "breakerHalfOpenFailures": "Required positive integer."
    },
    "telemetry": {
      "referer": "Required non-empty string URL that matches the App URL registered with your OpenRouter key.",
      "title": "Required non-empty string describing your application."
    }
  },
  "_environmentOverrides": {
    "ROUTERX_DEFAULT_MODEL": "Overrides defaultModel.",
    "ROUTERX_DEFAULT_BASE_URL": "Overrides defaultBaseUrl.",
    "ROUTERX_DEFAULT_SAVE_PATH": "Overrides defaultSavePath.",
    "ROUTERX_TIMEOUT": "Overrides timeout in milliseconds.",
    "ROUTERX_MAX_RETRIES": "Overrides maxRetries.",
    "ROUTERX_RESILIENCE_TIMEOUT_MS": "Overrides resilience.timeoutMs.",
    "ROUTERX_RESILIENCE_MAX_RETRIES": "Overrides resilience.maxRetries.",
    "ROUTERX_RESILIENCE_BASE_DELAY_MS": "Overrides resilience.baseDelayMs.",
    "ROUTERX_RESILIENCE_MAX_DELAY_MS": "Overrides resilience.maxDelayMs.",
    "ROUTERX_RESILIENCE_JITTER_MS": "Overrides resilience.jitterMs.",
    "ROUTERX_RESILIENCE_BREAKER_THRESHOLD": "Overrides resilience.breakerThreshold.",
    "ROUTERX_RESILIENCE_BREAKER_COOLDOWN_MS": "Overrides resilience.breakerCooldownMs.",
    "ROUTERX_RESILIENCE_BREAKER_HALF_OPEN_SUCCESSES": "Overrides resilience.breakerHalfOpenSuccesses.",
    "ROUTERX_RESILIENCE_BREAKER_HALF_OPEN_FAILURES": "Overrides resilience.breakerHalfOpenFailures.",
    "ROUTERX_HTTP_REFERER": "Overrides telemetry.referer.",
    "ROUTERX_CLIENT_TITLE": "Overrides telemetry.title."
  },
  "defaultModel": "openai/gpt-4o-mini",
  "defaultBaseUrl": "https://openrouter.ai/api/v1",
  "defaultSavePath": "./outputs",
  "maxRetries": 3,
  "timeout": 30000,
  "resilience": {
    "maxRetries": 3,
    "baseDelayMs": 1000,
    "maxDelayMs": 8000,
    "jitterMs": 250,
    "timeoutMs": 30000,
    "breakerThreshold": 5,
    "breakerCooldownMs": 60000,
    "breakerHalfOpenSuccesses": 1,
    "breakerHalfOpenFailures": 1
  },
  "telemetry": {
    "referer": "https://routerx.sh",
    "title": "RouterX CLI"
  }
}
